[project]
name = "chaptertool-scripts"
version = "0.1.0"
description = "Repository utility scripts for ChapterTool"
requires-python = ">=3.8"
dependencies = [
    "defusedxml>=0.7",
]

[tool.uv]
package = false
